{
  "name": "avoin-wmts-openlayers",
  "dependencies": {
    "ol": "10.5.0",
    "ol-mapbox-style": "^13.0.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "nyc": "^17.1.0",
    "vite": "^6.1.6",
    "vite-plugin-istanbul": "^7.2.1"
  },
  "scripts": {
    "start": "vite",
    "build": "node scripts/generate-osm-manifest.mjs && vite build",
    "dev": "vite",
    "predev": "node scripts/generate-osm-manifest.mjs",
    "test:e2e": "playwright test",
    "coverage": "nyc report --reporter=html --reporter=text-summary"
  }
}