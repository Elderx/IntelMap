{
  "name": "avoin-wmts-openlayers",
  "dependencies": {
    "chart.js": "^4.5.1",
    "flatpickr": "^4.6.13",
    "gpx-basic-stats": "^2.2.0",
    "mgrs": "^2.1.0",
    "mqtt": "^5.15.0",
    "ol": "10.5.0",
    "ol-mapbox-style": "^13.0.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "nyc": "^17.1.0",
    "vite": "^6.1.6",
    "vite-plugin-istanbul": "^7.2.1"
  },
  "scripts": {
    "start": "vite",
    "build": "node scripts/generate-osm-manifest.mjs && vite build",
    "dev": "vite",
    "predev": "node scripts/generate-osm-manifest.mjs",
    "test:e2e": "playwright test && nyc report --reporter=text --reporter=html",
    "coverage": "nyc report --reporter=html --reporter=text-summary"
  }
}
